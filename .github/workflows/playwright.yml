name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test --project=Chromium --workers=3

      - name: Run Playwright tests last failed
        run: npx playwright test --last-failed --project=Chromium --workers=3

      - name: Upload Playwright Report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: test-results
          path: test-results/
          retention-days: 2

      # - name: Notify Slack
      #   uses: slackapi/slack-github-action@v1.23.0
      #   with:
      #     channel-id: 'C08136M18D9'
      #     slack-message: |
      #       Playwright tests completed!
      #       ✅ Report link: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
      #       slack-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Upload report to Slack
        run: |
          curl -F file=@playwright-report/index.html \
               -F "channels=#test-reports" \
               -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
                 https://slack.com/api/files.upload
        